{
  "name": "chat-project",
  "version": "0.0.1",
  "repository": "git@github.com:AlexKomz/chat-project.git",
  "author": "AlexKomz <a.komzoltsev@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn lerna run build",
    "build:server": "yarn build --scope=@chat-project/server",
    "build:client": "yarn build --scope=@chat-project/client",
    "start": "yarn lerna run start",
    "start:server": "yarn start --scope=@chat-project/server",
    "start:client": "yarn start --scope=@chat-project/client"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
